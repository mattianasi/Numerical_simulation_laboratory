#include <iostream>
#include <fstream>
#include <cmath>
#include <cstdlib>
#include <vector>
#include "random.h"
#include "funzioni.h"

using namespace std;

double error(vector<double>, vector<double>, int);

int main(){
Random rand;
SeedSet(rand);

int M = 100000, N = 100, L = M/L;
vector<double> av(N), av2(N), err(N);

return 0;
}